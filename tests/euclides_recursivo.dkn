function mcd_recursivo(a, b) {
    if (b == 0) {
        retornar a;
    }
    // Caso recursivo: mcd(b, a % b)
    retornar mcd_recursivo(b, a % b);
}

print("Resultado Euclides Recursivo (48, 18):");
print(mcd_recursivo(48, 18));
