function mcd(a, b) {
    if (b == 0) {
        return a;
    }
    // Teorema: mcd(a, b) = mcd(b, a % b)
    return mcd(b, a % b);
}

resultado = mcd(48, 18); // El MCD es 6
print("El MCD de 48 y 18 es:");
print(resultado);
